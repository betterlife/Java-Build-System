1. Add the function to run the current file in vim(vim script may needed)
2. Add a property to decide whether to backup when deploying
3. Add the ability to judge windows or unix

Release notes 0.3:

1. Add target assemble to assemble a deployable package
2. Add target deploy to deploy to local application server
3. Add locale attribute for docs:javadoc target
4. Rename codeCoverage:instrument and codeCoverage:report to cc:instrument and cc:report
5. Add the follow settings
    jetty.home
    junit.home
    assemble.files.path
    assemble.path
    assemble.path.lib
    assemble.package.name
    assemble.package.version
    application.server.path
    application.server.lib.path
    application.deploy.backup
6. Add a directory lib for putting all business related jar files.
7. Add a directory app for putting all business related config files.
8. Add jetty support when compiling and running junit test cases.
9. Move script from root directory to tools
